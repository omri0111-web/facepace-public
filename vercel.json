{
  "buildCommand": "vite build --config vite.config.public.ts",
  "outputDirectory": "build",
  "framework": "vite",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
